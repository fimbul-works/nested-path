{
  "name": "@fimbul-works/nested-path",
  "description": "Small set of TypeScript utilities for reading, writing, checking, and deleting values at arbitrary depths inside objects and arrays",
  "version": "1.0.1",
  "type": "module",
  "private": false,
  "license": "MIT",
  "author": "FimbulWorks <contact@fimbul.works>",
  "homepage": "https://github.com/fimbul-works/nested-path#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fimbul-works/nested-path.git"
  },
  "bugs": {
    "url": "https://github.com/fimbul-works/nested-path/issues"
  },
  "keywords": [
    "nested",
    "nested-property",
    "deep-property",
    "object-path",
    "array-path",
    "path-traversal",
    "nested-object",
    "typescript"
  ],
  "main": "./dist/cjs/index.js",
  "module": "./dist/esm/index.js",
  "types": "./dist/esm/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/esm/index.d.ts",
      "import": "./dist/esm/index.js",
      "require": "./dist/cjs/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "sideEffects": false,
  "scripts": {
    "clean": "rm -rf dist",
    "clean:bundles": "rm -rf bundles",
    "build": "pnpm clean && pnpm build:esm && pnpm build:cjs && pnpm build:docs",
    "build:esm": "tsc -p tsconfig.json",
    "build:cjs": "tsc -p tsconfig.cjs.json && echo '{\"type\":\"commonjs\"}' > dist/cjs/package.json",
    "build:bundles": "pnpm clean:bundles && tsdown",
    "build:all": "pnpm build && pnpm build:bundles && pnpm sizes",
    "build:docs": "typedoc",
    "format": "npx biome format --write src",
    "test": "vitest",
    "sizes": "bundle-size",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@fimbul-works/bundle-size": "link:../bundle-size",
    "tsdown": "^0.22.0",
    "typedoc": "^0.28.19",
    "typedoc-plugin-markdown": "^4.11.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
